{
  "name": "tusk-web",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run copy-wasm && astro dev",
    "build": "npm run copy-wasm && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "copy-wasm": "shx mkdir -p public/pkg && shx cp -r ../pk/* public/pkg/"
  },
  "dependencies": {
    "astro": "^6.1.8"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
